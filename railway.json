{
    "build": {
        "builder": "NIXPACKS",
        "nixpacksConfig": {
            "phpExtensions": ["pdo_mysql", "mysqli"],
            "node": {
                "installCmd": "rm -f package-lock.json && npm install"
            }
        }
    }
}
